# config/filco_bt.conf
# Minimal safe config for Filco BT shield (no deprecated symbols)

CONFIG_ZMK_KEYBOARD_NAME="filco_bt"
CONFIG_ZMK_KEYBOARD=y

# Core features
CONFIG_ZMK_MATRIX=y
CONFIG_I2C=y
CONFIG_GPIO=y

# Don't include deprecated/unknown symbols here (no KSCAN, no ZMK_KSCAN_COMPOSITE)
